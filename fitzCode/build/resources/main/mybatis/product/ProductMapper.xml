<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.co.fitzcode.product.mapper.ProductMapper">
    <select id="getProductsByPage" parameterType="java.util.HashMap" resultType="kr.co.fitzcode.product.dto.ProductDTO">
        SELECT product_id, product_name, description, brand, price, stock, category_id, image_url, created_at
        FROM PRODUCT
        ORDER BY product_id
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="getAllProducts" resultType="kr.co.fitzcode.product.dto.ProductDTO">
        SELECT product_id, product_name, description, brand, price, stock, category_id, image_url, created_at
        FROM PRODUCT
    </select>

    <select id="getProductById" resultType="kr.co.fitzcode.product.dto.ProductDTO" parameterType="int">
        SELECT product_id, product_name, description, brand, price, stock, category_id, image_url, created_at
        FROM PRODUCT
        WHERE product_id = #{productId}
    </select>

    <insert id="insertProduct" parameterType="kr.co.fitzcode.product.dto.ProductDTO">
        INSERT INTO PRODUCT
        (product_name, description, brand, price, stock, category_id, image_url, created_at)
        VALUES
        (#{productName}, #{description}, #{brand}, #{price}, #{stock}, #{categoryId}, #{imageUrl}, NOW())
    </insert>

    <insert id="insertManyProducts" parameterType="java.util.List">
        INSERT INTO PRODUCT
        (product_name, description, brand, price, stock, category_id, image_url, created_at)
        VALUES
        <foreach collection="list" item="product" separator=",">
            (#{product.productName}, #{product.description}, #{product.brand},
            #{product.price}, #{product.stock}, #{product.categoryId},
            #{product.imageUrl}, NOW())
        </foreach>
    </insert>


    <update id="updateProduct" parameterType="kr.co.fitzcode.product.dto.ProductDTO">
        UPDATE PRODUCT
        <set>
            <if test="product_name != null">
                product_name = #{productName},
            </if>
            <if test="description != null">
                description = #{description},
            </if>
            <if test="brand != null">
                brand = #{brand},
            </if>
            <if test="price != null">
                price = #{price},
            </if>
            <if test="stock != null">
                stock = #{stock},
            </if>
            <if test="category_id != null">
                category_id = #{categoryId},
            </if>
            <if test="image_url != null">
                image_url = #{imageUrl},
            </if>
        </set>
        WHERE product_id = #{productId};
    </update>


    <delete id="deleteProductById" parameterType="int">
        DELETE * FROM PRODUCT
        WHERE product_id = #{productId}
    </delete>


    <select id="getNumberOfProducts" resultType="int">
        SELECT COUNT(*) FROM PRODUCT;
    </select>

    <select id="getProductsByFilter" resultType="kr.co.fitzcode.product.dto.ProductDTO" parameterType="java.util.Map">
        SELECT * FROM PRODUCT p CATEGORY c
        JOIN ON
        
        <where>
            <foreach collection="filters" item="entry" separator="AND">
                ${entry.key} = #{entry.value}
            </foreach>
        </where>
    </select>




</mapper>